% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glimpse_df.R
\name{glimpse_param_visits}
\alias{glimpse_param_visits}
\title{Glimpse parameter and visit columns.}
\usage{
glimpse_param_visits(df, id_cols, param_visit_cols)
}
\arguments{
\item{df}{\code{data.frame} - contains ID and parameter/visit columns.}

\item{id_cols}{\code{character} - ID column(s) to identify subjects
e.g. c("USUBJID").}

\item{param_visit_cols}{\code{character} - parameter and visit column names
e.g. c("PARAM", "PARAMCD", "AVISIT", "AVISITN").}
}
\value{
\code{summary_param_visits} - contains deduplicated profiles with masked
counts and the unique param/visit combinations (after masking).
}
\description{
Extracts per-subject param/visit profiles from a BDS dataset. Rare
param/visit combinations (count = 1) are masked for privacy protection.
Subjects are grouped by their set of surviving param/visit combinations
to form profiles, which are used by \code{\link[=simulate_bds]{simulate_bds()}} to construct the
simulation spine.
}
\examples{
\dontshow{
# Load a bundled ADLB fixture so the example can run.
adlb <- read.csv(
  system.file("extdata", "adlb.csv", package = "synadam"),
  stringsAsFactors = FALSE
)
}
# Build per-subject parameter/visit profiles (used within glimpse_bds()).
glimpse_param_visits(
  adlb,
  id_cols = "USUBJID",
  param_visit_cols = c("PARAM", "PARAMCD", "AVISIT", "AVISITN")
)

}
