% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glimpse_study.R
\name{glimpse_study}
\alias{glimpse_study}
\title{Glimpse all ADaM datasets for a study.}
\usage{
glimpse_study(config_path, study_summary_path)
}
\arguments{
\item{config_path}{\code{character} - path to YAML configuration file. Has the
same structure as the one consumed by \code{\link[=simulate_study]{simulate_study()}}.}

\item{study_summary_path}{\code{character} - path to write the study summary
\code{.rds} file. The parent directory will be created if it does not exist.}
}
\value{
\code{NULL} (invisibly). The study summary is a named list with elements
\code{summaries} (named list of glimpse summary objects, keyed by dataset
name from the YAML), \code{seed}, \code{synadam_version}, and \code{glimpsed_at}.
}
\description{
Reads the YAML configuration used by \code{\link[=simulate_study]{simulate_study()}}, runs the
appropriate \verb{glimpse_*()} for each dataset, and saves the collected
summaries (plus seed and version metadata) to a single \code{.rds} file.
}
\details{
The resulting study summary can later be passed to
\code{\link[=simulate_study_from_summary]{simulate_study_from_summary()}} to generate the synthetic datasets,
decoupling the glimpse phase (which requires access to real \code{.sas7bdat}
files) from the simulate phase.
}
\examples{
\dontshow{
# Stage ADaM datasets as SAS files so the example can run.
adam_dir <- file.path(tempdir(), "adam_dir")
dir.create(adam_dir, showWarnings = FALSE)
adsl <- read.csv(
  system.file("extdata", "adsl.csv", package = "synadam"),
  stringsAsFactors = FALSE
)
suppressWarnings(
  haven::write_sas(adsl, file.path(adam_dir, "adsl.sas7bdat"))
)
}
# Generate a config for the staged ADaM datasets.
yaml_path <- generate_study_config(
  adam_dir,
  output_dir = file.path(tempdir(), "syn_glimpse_out")
)

# Glimpse phase: write the study summary (decoupled from the SAS files).
summary_path <- tempfile(fileext = ".rds")
glimpse_study(yaml_path, summary_path)

# Simulate phase: generate synthetic datasets from the study summary.
out_dir <- file.path(tempdir(), "syn_glimpse_out")
simulate_study_from_summary(summary_path, out_dir)
list.files(out_dir, pattern = "\\\\.rds$")

}
\seealso{
\code{\link[=simulate_study_from_summary]{simulate_study_from_summary()}}, \code{\link[=simulate_study]{simulate_study()}}.
}
