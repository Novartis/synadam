% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_study.R
\name{simulate_study_from_summary}
\alias{simulate_study_from_summary}
\title{Simulate synthetic ADaM datasets from a previously-saved study summary.}
\usage{
simulate_study_from_summary(study_summary_path, output_dir, seed = NULL)
}
\arguments{
\item{study_summary_path}{\code{character} - path to the \code{.rds} study summary
written by \code{\link[=glimpse_study]{glimpse_study()}}.}

\item{output_dir}{\code{character} - directory to write \verb{syn_\{key\}.rds} files
to. Created if missing.}

\item{seed}{\code{integer} or \code{NULL} - simulation seed. When \code{NULL} (default), the
seed stored in the study summary is used; if the study summary has no seed,
it falls back to \code{123}. Override to draw a different synthetic replicate
from the same summaries.}
}
\value{
\code{NULL} (invisibly). Synthetic datasets are saved as
\verb{syn_\{key\}.rds} files in \code{output_dir}. Each dataset has a
\code{synadam_version} attribute.
}
\description{
Reads a study summary written by \code{\link[=glimpse_study]{glimpse_study()}} and produces synthetic
datasets, decoupling simulation from the original \code{.sas7bdat} files.
}
\examples{
\dontshow{
# Stage ADaM datasets as SAS files so the example can run.
adam_dir <- file.path(tempdir(), "adam_dir")
dir.create(adam_dir, showWarnings = FALSE)
adsl <- read.csv(
  system.file("extdata", "adsl.csv", package = "synadam"),
  stringsAsFactors = FALSE
)
suppressWarnings(
  haven::write_sas(adsl, file.path(adam_dir, "adsl.sas7bdat"))
)
}
# Generate a config and glimpse the datasets into a study summary.
yaml_path <- generate_study_config(
  adam_dir,
  output_dir = file.path(tempdir(), "syn_data")
)
study_summary_path <- tempfile(fileext = ".rds")
glimpse_study(yaml_path, study_summary_path)

# Simulate synthetic datasets from the saved study summary.
out_dir <- file.path(tempdir(), "syn_data")
simulate_study_from_summary(study_summary_path, out_dir)
list.files(out_dir, pattern = "\\\\.rds$")

}
\seealso{
\code{\link[=glimpse_study]{glimpse_study()}}, \code{\link[=simulate_study]{simulate_study()}}.
}
