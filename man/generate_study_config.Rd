% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_study_config.R
\name{generate_study_config}
\alias{generate_study_config}
\title{Generate a draft study config YAML from a directory of ADaM files.}
\usage{
generate_study_config(
  adam_dir,
  output_dir,
  config_yaml_name = "synadam_config.yaml",
  seed = 123
)
}
\arguments{
\item{adam_dir}{\code{character(1)} - directory containing \code{.sas7bdat} files.}

\item{output_dir}{\code{character(1)} - directory that both the generated config
YAML and the synthetic \code{.rds} outputs are written to. Created recursively
if it does not exist. This same path is recorded as the top-level
\code{output_dir} field in the YAML, so \code{simulate_study()} writes its
\code{syn_*.rds} files alongside the config.}

\item{config_yaml_name}{\code{character(1)} - filename for the generated config
within \code{output_dir}. Defaults to \code{"synadam_config.yaml"}. If the file
already exists, it is silently overwritten.}

\item{seed}{\code{integer(1)} / \code{numeric(1)} or \code{NULL} - value to write for
the top-level \code{seed} field in the YAML. Defaults to \code{123}. If \code{NULL}, a
\code{"REVIEW: ..."} placeholder is written instead.}
}
\value{
\code{character(1)} - the path the YAML was written to
(\code{file.path(output_dir, config_yaml_name)}).
}
\description{
Scans a directory of \code{.sas7bdat} files, infers \code{dataset_type} and
per-dataset glimpse arguments using CDISC filename and column-name
conventions, and writes a draft YAML config suitable for \code{simulate_study()}.
Inferred fields are tagged with inline \verb{# REVIEW} comments where the signal
is weak so users know what to verify before running the simulation.
}
\details{
Only files whose basename matches \code{ad*.sas7bdat} (case-insensitive) are
considered. Any other files in \code{adam_dir} are skipped with a single
warning listing them.
}
\section{Detection rules}{

Filename match (case-insensitive, basename without extension):
\itemize{
\item starts with \code{adsl} -> \code{adsl}
\item starts with \code{adae}/\code{adcm}/\code{admh}/\code{addv} -> \code{occds}
\item starts with \code{adtte} -> \code{tte}
\item any other name (including \verb{ad*}) -> fall through to column inspection
}

Column inspection rules (checked in order; \code{USUBJID} is required in
every ADaM dataset, so it is not used as a discriminator). ADSL is
classified from the filename only - any non-\verb{adsl*} file that lacks
BDS/TTE/OCCDS column signatures falls through to \code{"REVIEW"}:
\itemize{
\item has \code{PARAM} -> \code{bds} (checked before CNSR so oncology hybrid
datasets like ADRECIST that carry TTE-derived parameters inline
classify as the longitudinal BDS they actually are)
\item has \code{CNSR} -> \code{tte} (CNSR is the only structurally required TTE
variable in ADaM; \code{EVNTDESC} is conventional but not universal)
\item has any \verb{*SEQ} column -> \code{occds}
\item otherwise -> \code{"REVIEW"} placeholder + warning
}

Errors if no ADSL file is detected; \code{simulate_study()} requires exactly
one. If multiple ADSL candidates are detected, a file named exactly
\code{adsl.sas7bdat} (case-insensitive) is preferred; otherwise the
alphabetically-first candidate is used. The picked file is kept and the
rest are dropped with a warning.
}

\section{Column-role detection}{

Within a classified dataset, the column-role inference picks up:
\itemize{
\item \code{id_cols}: \code{USUBJID} and \code{SUBJID} when present.
\item \code{treatment_cols} (ADSL): \code{ACTARM}, \code{ACTARMCD}, \code{ARM}, \code{ARMCD},
\code{TRTSEQP}, \code{TRTSEQA} and any
\code{TRT##A}/\code{TRT##AN}/\code{TRT##AL}/\code{TRT##P}/\code{TRT##PN}/\code{TRT##PL} matches,
plus \code{TRT##(A|P)GR\\d*} and \code{TRT##(A|P)GR\\d*N} group variants.
\item \code{visit_cols} (BDS): \code{AVISIT}, \code{AVISITN}, \code{VISIT}, \code{VISITNUM},
\code{ATPT}, \code{ATPTN} when present.
\item \code{param_cols} (BDS, TTE): \code{PARAM}, \code{PARAMCD}, and any \verb{PARCAT*} columns.
\item \code{seq_col} (OCCDS): the first known domain-coded \verb{*SEQ} column
(\code{AESEQ}, \code{CMSEQ}, \code{MHSEQ}, \code{PRSEQ}, \code{EXSEQ}, \code{DSSEQ}); falls back
to the first \verb{*SEQ} column if none of those are present.
\item \code{censor_cols} (TTE): \code{CNSR}, \code{EVNTDESC}, \code{CNSDTDSC} - only emitted
when two or more of these are detected. A single \code{CNSR} flows
through as a regular column and \code{censor_cols} is omitted.
\item \code{ordered_col_sets}: X/XN/XL groups where two or three variants of a
base column name co-exist.
}
}

\section{What is NOT auto-inferred}{

Some config decisions are opinionated and intended to be pruned or
extended by the user after generation:
\itemize{
\item \code{flag_cols} emits every \verb{*FL} column found. The user is expected to
trim this list down to the flags they want to preserve.
\item Non-FL flag-like indicators (e.g. \code{AESER}) are not detected.
\item Ordered column sets that share semantics but not naming (e.g.
\code{AEBODSYS}/\code{AEDECOD}) are not auto-paired; add them by hand.
\item For BDS and TTE datasets \code{param_cols} auto-includes \code{PARAM},
\code{PARAMCD}, and any \verb{PARCAT*} columns present, but NOT \code{PARAMTYP}
or \code{PARAMN}; add these manually if relevant.
\item For BDS, OCCDS, and TTE datasets \code{ordered_col_sets} excludes any
column also present in the chosen ADSL. ADSL-shared columns are
pulled in via the ADSL spine at simulation time, so listing them
here would duplicate the column.
}
}

\examples{
\dontshow{
# Stage ADaM datasets as SAS files so the example can run.
adam_dir <- file.path(tempdir(), "adam_dir")
dir.create(adam_dir, showWarnings = FALSE)
adsl <- read.csv(
  system.file("extdata", "adsl.csv", package = "synadam"),
  stringsAsFactors = FALSE
)
suppressWarnings(
  haven::write_sas(adsl, file.path(adam_dir, "adsl.sas7bdat"))
)
adae <- read.csv(
  system.file("extdata", "adae.csv", package = "synadam"),
  stringsAsFactors = FALSE
)
suppressWarnings(
  haven::write_sas(adae, file.path(adam_dir, "adae.sas7bdat"))
)
}

yaml_path <- generate_study_config(
  adam_dir,
  output_dir = file.path(tempdir(), "syn_out")
)
yaml_path

}
