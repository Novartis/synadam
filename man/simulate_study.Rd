% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_study.R
\name{simulate_study}
\alias{simulate_study}
\title{Simulate synthetic ADaM datasets for a study.}
\usage{
simulate_study(config_path)
}
\arguments{
\item{config_path}{\code{character} - path to YAML configuration file.}
}
\value{
\code{NULL} (invisibly). Synthetic datasets are saved as individual
\code{.rds} files named \verb{syn_\{key\}.rds} in the \code{output_dir} directory, where
\code{{key}} is the dataset name from the YAML config. Each dataset has a
\code{synadam_version} attribute containing the package version.
}
\description{
Taking as input a yaml configuration that specifies the ADaM datasets
for a study of interest, this function will simulate synthetic versions of
each and save them as individual .rds files in a specified directory.
}
\details{
The YAML configuration file should have the following structure:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{output_dir: "/path/to/output_directory"
seed: 32

datasets:
  adsl:
    dataset_type: "adsl"
    path: "/path/to/adsl.sas7bdat"
    id_cols: ["USUBJID", "SUBJID"]
    treatment_cols: ["TRT01A", "TRT01AN"]
    flag_cols: ["SAFFL", "ITTFL"]
    ordered_col_sets:
      - ["REGION1", "REGION1N"]

  adlb:
    dataset_type: "bds"
    path: "/path/to/adlb.sas7bdat"
    id_cols: ["USUBJID"]
    param_cols: ["PARAM", "PARAMCD"]
    visit_cols: ["AVISIT", "AVISITN"]
    flag_cols: ["ANL01FL"]

  adtte:
    dataset_type: "tte"
    path: "/path/to/adtte.sas7bdat"
    param_cols: ["PARAM", "PARAMCD"]
    censor_cols: ["CNSR", "EVNTDESC", "CNSDTDSC"]
    flag_cols: ["ANL01FL"]
    ordered_col_sets:
      - ["SRCDOM", "SRCVAR"]

  adae:
    dataset_type: "occds"
    path: "/path/to/adae.sas7bdat"
    id_cols: ["USUBJID"]
    seq_col: "AESEQ"
    flag_cols: ["AOCCFL", "TRTEMFL"]
    ordered_col_sets:
      - ["AEBODSYS", "AEDECOD"]
}\if{html}{\out{</div>}}

Dataset names in the output files are determined by the YAML keys under
\verb{datasets:} (e.g., \code{syn_adsl.rds}, \code{syn_adlb.rds}). Exactly one dataset
must have \code{dataset_type: "adsl"}. The \code{output_dir} directory will be
created automatically if it doesn't exist.
}
\examples{
\dontshow{
# Stage ADaM datasets as SAS files so the example can run.
adam_dir <- file.path(tempdir(), "adam_dir")
dir.create(adam_dir, showWarnings = FALSE)
adsl <- read.csv(
  system.file("extdata", "adsl.csv", package = "synadam"),
  stringsAsFactors = FALSE
)
suppressWarnings(
  haven::write_sas(adsl, file.path(adam_dir, "adsl.sas7bdat"))
)
adae <- read.csv(
  system.file("extdata", "adae.csv", package = "synadam"),
  stringsAsFactors = FALSE
)
suppressWarnings(
  haven::write_sas(adae, file.path(adam_dir, "adae.sas7bdat"))
)
}

out_dir <- file.path(tempdir(), "syn_study")
yaml_path <- generate_study_config(adam_dir, output_dir = out_dir)

# Simulate the whole study; synthetic syn_*.rds files land in out_dir.
simulate_study(yaml_path)
list.files(out_dir, pattern = "\\\\.rds$")

}
