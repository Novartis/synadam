% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adam_adsl.R
\name{simulate_adsl}
\alias{simulate_adsl}
\title{Simulate a synthetic ADSL.}
\usage{
simulate_adsl(adsl_summary, seed = NULL)
}
\arguments{
\item{adsl_summary}{\code{summary_adsl} - contains summary objects that describe
an ADSL dataset. Created by \code{\link[=glimpse_adsl]{glimpse_adsl()}}.}

\item{seed}{\code{integer} - random seed for reproducibility.}
}
\value{
\code{data.frame} - synthetic ADSL with the same columns (and column
order) as the dataset that was glimpsed, and one row per subject.
}
\description{
Generates synthetic subject-level data from an ADSL summary, preserving
the structure of the original dataset.
}
\examples{
\dontshow{
# Load a bundled ADSL fixture so the example can run.
adsl <- read.csv(
  system.file("extdata", "adsl.csv", package = "synadam"),
  stringsAsFactors = FALSE
)
}
adsl_summary <- glimpse_adsl(
  adsl,
  id_cols = c("USUBJID", "SUBJID"),
  treatment_cols = c("TRT01A", "TRT01AN"),
  flag_cols = "SAFFL"
)
syn_adsl <- simulate_adsl(adsl_summary, seed = 42)
head(syn_adsl)

}
